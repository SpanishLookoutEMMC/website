{
  "name": "sl-emmc-website",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "fetch-sermons": "node scripts/fetch-sermons.mjs",
    "dev": "node build.mjs && node --watch build.mjs & node serve.mjs"
  },
  "devDependencies": {
    "marked": "^18.0.2"
  }
}
